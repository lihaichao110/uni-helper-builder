# Uni Builder 项目级 Codex 规则

## 适用范围与优先级

- 本文件适用于整个仓库；进入 `backend/` 或 `frontend/` 工作时，必须同时读取并遵守对应目录中的 `AGENTS.md`。
- 越接近目标文件的 `AGENTS.md` 规则优先级越高；若规则冲突，采用更具体、更严格且不违背用户要求的规则。
- 所有面向用户的说明、计划、变更摘要和仓库文档使用中文；代码标识符、协议字段和第三方 API 名称使用英文。

## 通用工程要求

- 修改前先阅读相关入口、配置、测试和相邻模块，保持现有行为与接口兼容，除非任务明确要求破坏性变更。
- 坚持最小充分改动：不顺手重构无关模块，不覆盖用户未提交的改动，不提交缓存、日志、数据库、构建产物或本地环境文件。
- 禁止硬编码密钥、Token、密码、私钥和生产地址；新增配置必须通过环境变量或受控配置注入，并同步更新 `.env.example`。
- 所有外部输入都要校验；权限、文件路径、命令执行、网络访问和反序列化属于高风险边界，必须采用最小权限与明确白名单。
- 异常不得静默吞掉。可恢复错误应记录必要上下文并返回稳定错误；不可恢复错误应保留异常链，日志中不得泄露凭据和敏感数据。
- 新增或修改行为必须补充相应测试。修复缺陷时优先添加能复现缺陷的测试，再实现修复。
- 引入依赖前确认现有能力无法合理完成需求；依赖应固定版本、说明用途，并更新对应锁文件或依赖清单。

## 目录结构同步规则

- `docs/PROJECT_STRUCTURE.md` 是当前目录职责的唯一说明文档。
- 新增、删除、移动、重命名目录，或改变现有目录/关键文件职责时，必须在同一任务内同步更新该文档。
- 文档必须描述实际存在的结构；规划中的目录只能写入“目标结构”，不得伪装成当前结构。
- 任务结束前重新核对目录树。Codex 只能保证其参与的变更实时同步；发现人工变更导致文档过期时，应在当前任务中一并校正。

## 完成标准

- 相关代码完成格式化、静态检查、类型检查和测试后，任务才可视为完成。
- 默认执行以下检查；若仅修改文档，可跳过与代码无关的检查，但仍需验证链接和目录描述：

```powershell
python scripts/check_source_size.py
cd backend
python -m ruff format --check .
python -m ruff check .
python -m mypy app
python -m pytest -q
cd ../frontend
npm run check
npm run build
```

- 若某项检查因环境或外部服务无法执行，必须明确说明未验证项、原因和风险，不得声称全部通过。
